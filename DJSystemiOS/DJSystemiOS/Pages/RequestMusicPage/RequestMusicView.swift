import SwiftUI

struct RequestMusicView: View {
    var body: some View {
        Text("RequestMusicView")
    }
}

struct RequestMusicView_Previews: PreviewProvider {
    static var previews: some View {
        RequestMusicView()
    }
}
